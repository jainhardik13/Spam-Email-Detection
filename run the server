for running on local machine, use the following command in the terminal:
uvicorn app:app --reload

# for running on the server, use the following command in the terminal:
uvicorn app:app --host 0.0.0.0 --port $PORT
